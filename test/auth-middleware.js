const authMiddleware = require('../middleware/auth');
//check for tests (success or fail)
const expect = require('chai').expect;

it("Should throw an error if Authentication token is not exist", function() {
    
})